services:
  - type: web
    name: banco
    env: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "gunicorn banco.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: "3.14.3"
